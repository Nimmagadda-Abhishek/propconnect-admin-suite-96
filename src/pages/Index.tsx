// PropConnect Admin Panel - Main Dashboard
// This file is no longer used as the main entry point
// The dashboard is now handled by the Dashboard component

const Index = () => {
  // Redirect to dashboard is handled by App.tsx routing
  return null;
};

export default Index;
